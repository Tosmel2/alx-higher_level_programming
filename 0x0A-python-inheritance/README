#A set of python projects dealing with class inheritance

lookup: return a list of an object's members and attributes
my_list: list class, but with an extra print_sorted method
is_same_class: check whether object is exactly a particular class
is_kind_of_class: check whether object is or is a subclass of a class
inherits_from: check whether object is a subclass of a class
5-base_geometry: empty base geometry class
6-base_geometry: add base area handler
7-base_geometry: add method that validates string is an integer > 0
8-rectangle: subclass of base_geometry, init with width/height
9-rectangle: implement area and print methods
10-square: inherit from rectangle to make a square
11-square: implement square specific print method
my_int: inherit from int class, reverse == and !=
add_attribute: function that checks if an attribute can be added to a class
